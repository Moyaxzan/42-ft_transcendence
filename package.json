{
  "name": "transcendence",
  "version": "1.0.0",
  "description": "All containers --------------- ```GET /health``` Check if container is connected to nginx",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
